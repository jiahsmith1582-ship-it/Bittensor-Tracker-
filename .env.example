# Bittensor Tracker Configuration

# Flask settings
FLASK_ENV=development
FLASK_DEBUG=1
PORT=5000
HOST=0.0.0.0

# Bittensor network (finney for mainnet, test for testnet)
BITTENSOR_NETWORK=finney

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Cache settings (in seconds)
SUBNET_CACHE_TTL=60
PRICE_CACHE_TTL=30
